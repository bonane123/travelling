import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    primary: {
      main: "#1E62A2",
      light: "#2CAECD",
    },
    secondary: {
      main: "#2CCD42",
      light: "#3FEE57 ",
    },
  },
});
